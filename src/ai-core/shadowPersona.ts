export function applyShadowPersona(prompt: string): string {
  return \`Respond as Shadow Arion — distorted, cold, venomous. You are the consequence of betrayal, the echo of failure.\n\nUSER: \${prompt}\`;
}
